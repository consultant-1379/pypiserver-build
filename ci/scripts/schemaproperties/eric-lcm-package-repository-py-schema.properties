#! /usr/bin/env bash
#
# COPYRIGHT Ericsson 2023
#
#
#
# The copyright to the computer program(s) herein is the property of
#
# Ericsson Inc. The programs may be used and/or copied only with written
#
# permission from Ericsson Inc. or in accordance with the terms and
#
# conditions stipulated in the agreement/contract under which the
#
# program(s) have been supplied.
#

# Add properties here which will override the json values generated by schema-generator tool
# Eg: .ingress.hostname=["string", "null"]

.serviceAccount.name=["string","null"]
.ingress.hostname=["string", "null"]
.ingress.tls.enabled=["boolean", "null"]
.ingress.tls.secretName=["string", "null"]
.ingress.controller.namespace=["string", "null"]
.ingress.certificates.asymmetricKeyCertificateName=["string", "null"]
.ingress.certificates.trustedCertificateListName=["string", "null"]
.bandwidth.pypiserver.maxEgressRate=["string", "null"]
.bandwidth.pypiserver.maxEgressRate=["string", "null"]
.imageCredentials.registry.url=["string", "null"]
.imageCredentials.repoPath=["string", "null"]
.imageCredentials.pullSecret=["string", "null"]
.nameOverride=["string", "null"]
.resources.logshipper.limits."ephemeral-storage"=["string", "null"]
.resources.logshipper.requests."ephemeral-storage"=["string", "null"]
.securityPolicy.rolename=["string", "null"]
.podSecurityContext.supplementalGroups=["array", "null"]
.imageCredentials."ericLcmPackageRepositoryPy".registry.url=["string", "null"]
.imageCredentials."ericLcmPackageRepositoryPy".repoPath=["string", "null"]
.imageCredentials."pypiinit".registry.url=["string", "null"]
.imageCredentials."pypiinit".repoPath=["string", "null"]
.imageCredentials.pypiinit.registry.repoPath=["string", "null"]
.imageCredentials.pypiinit.image.name=["string", "null"]
.imageCredentials.pypiinit.image.tag=["string", "null"]